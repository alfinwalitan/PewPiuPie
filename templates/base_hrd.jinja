<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Dashboard - HRD{% endblock %}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='base_hrd.css') }}">
    
    {% block extra_css %}{% endblock %}
</head>
<body class="body">
    <header>
        <div class="logo">Jobbers</div>
        <div class="menu">
            <a href="{{ url_for('dashboardhrd') }}" class="dashboard-btn">🏠 Dashboard</a>
            <span class="btn-nav user-icon">👤 {{ user_name }}</span>
        </div>
    </header>

    <div class="container">
        {% block content %}{% endblock %}
    </div>
</body>
</html>